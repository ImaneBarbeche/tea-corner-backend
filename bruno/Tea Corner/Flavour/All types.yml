info:
  name: All types
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/flavour-type/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
