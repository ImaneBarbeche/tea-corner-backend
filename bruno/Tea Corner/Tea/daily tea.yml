info:
  name: daily tea
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/tea/daily
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
