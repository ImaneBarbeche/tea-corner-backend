info:
  name: Delete account (soft)
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/user/account
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
