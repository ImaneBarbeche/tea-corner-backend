info:
  name: Find all for users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/ingredient/all?type=herb
  params:
    - name: type
      value: herb
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Using the search parameter is optionnal
