info:
  name: Sign up
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/signup
  body:
    type: json
    data: |-
      {
        "display_name": "Imane",
        "user_name": "imanetealover",
        "email": "barbecheimane@gmail.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
