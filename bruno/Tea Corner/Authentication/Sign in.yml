info:
  name: Sign in
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/auth/signin
  body:
    type: json
    data: |-
      {
        "user_name": "minou",
        "password": "12341234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
