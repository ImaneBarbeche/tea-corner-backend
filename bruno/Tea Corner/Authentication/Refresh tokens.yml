info:
  name: Refresh tokens
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/auth/refresh-tokens
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
