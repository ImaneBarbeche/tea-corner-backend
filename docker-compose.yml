version: '3.9'

services:
  backend:
    build: .
    container_name: teacorner-backend
    ports:
      - "3000:3000"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      DB_NAME: teacorner
    depends_on:
      - postgres
    networks:
      - teacorner-network

  postgres:
    image: postgres:16
    container_name: teacorner-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: teacorner
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - teacorner-network

networks:
  teacorner-network:

volumes:
  pgdata:
